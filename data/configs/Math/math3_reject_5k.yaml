t2i_pretrain:
  dataset_names:
  - math3_reject_5k_new
  image_transform_args:
    image_stride: 16
    max_image_size: 1024
    max_image_size: 1024
    min_image_size: 512
  is_mandatory: true
  num_used_data: # The sum should be larger that NUM_GPUS x NUM_WORKERS
  - 5000
  weight: 1